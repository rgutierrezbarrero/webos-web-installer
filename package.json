{
  "name": "webos-web-installer",
  "version": "0.1.0",
  "description": "Web UI + helper para instalar apps en LG webOS usando ares",
  "main": "src/agent.js",
  "scripts": {
    "start": "node src/agent.js",
    "dev:serve": "npx http-server web -p 8080",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=16"
  }
}